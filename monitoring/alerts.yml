groups:
  - name: WaferAIAlerts
    rules:
      - alert: HighPreprocessingLatency
        expr: process_time_seconds_sum / process_time_seconds_count > 1
        for: 5m
        labels:
          severity: warning
        annotations:
          summary: "High preprocessing latency detected"
